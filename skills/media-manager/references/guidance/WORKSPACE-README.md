# 工作区 Guidance（个人）

此目录属于你的 **MediaManager 工作区**，存放选题/写作/发布经验。

- **可自由修改、新增**（如 `writing/podcast_interview_sop.md`）
- Mode A（在源码 repo 根开发）：本目录 **不会** 被 git 跟踪
- 默认模板来自 `skills/media-manager/references/guidance/`，`media setup` 只补充**缺失**文件，不覆盖已有内容

若要更新**默认模板**供所有用户参考，请改 canonical 源（`skills/media-manager/references/guidance/`）并提 PR，而不是只改本目录。

内容族与 guidance 加载规则见 **media-manager** skill 内的 `references/platform-families.md`（安装后位于 agent skills 目录下的 `media-manager/references/platform-families.md`）。
