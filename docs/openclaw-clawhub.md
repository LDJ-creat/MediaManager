# OpenClaw / ClawHub（可选）

可将 `skills/media-manager` 作为独立 skill 发布到 [ClawHub](https://clawhub.com)。

```bash
# 维护者预览
./scripts/sync-clawhub.sh --dry-run

# 用户安装（发布后）
clawhub install media-manager
```

与 `npx skills add LDJ-creat/MediaManager --skill media-manager` 并行，面向 OpenClaw 生态。
